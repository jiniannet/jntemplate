version: 2.1.{build}
image: Visual Studio 2019
configuration: Release
platform: Any CPU
before_build:
  - cmd: dotnet --version
  - cmd: dotnet restore
build_script:
  - cmd: dotnet build
  - cmd: dotnet test